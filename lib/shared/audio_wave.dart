import 'package:flutter/material.dart';

class AudioWave extends StatelessWidget {
  const AudioWave({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Audio Wave');
  }
}
